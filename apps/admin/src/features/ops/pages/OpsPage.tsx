import { PagePlaceholder } from '@/shared/ui/PagePlaceholder';

/** A01 global-nav target — content module is out of A01 scope. */
export function OpsPage() {
  return (
    <PagePlaceholder
      title="عملیات جاری"
      description="Workspace عملیات جاری در ماژول‌های بعدی طراحی می‌شود. این مسیر فقط برای ناوبری A01 نگه داشته شده است."
    />
  );
}
