import { PagePlaceholder } from '@/shared/ui/PagePlaceholder';

export function PlanningPage() {
  return (
    <PagePlaceholder
      title="برنامه‌ریزی"
      description="Placeholder برای Planning Workspace. Map-heavy UI بعداً روی shared/map لایه‌بندی می‌شود."
    />
  );
}
